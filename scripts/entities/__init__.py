"""
Targeted entity extractors for the Poliscopic entity graph.

Modules:
  cases.py    — case number extraction (jurisdiction-aware patterns)
  people.py   — person extraction (applicants, attorneys, planners, staff)
  firms.py    — firm/organization/vendor extraction (TBD)
  
  extract.py  — orchestration runner + shared utilities (KNOWN_ORGANIZATIONS, DB helpers)
  models/     — ML experiment code (discover, predict, classify, train)
"""
